import 'package:flutter/material.dart';

class CustomDecorations {
  final InputDecoration name = const InputDecoration(
      labelText: "Name", focusColor: Colors.indigo, alignLabelWithHint: true);
  final InputDecoration email = const InputDecoration(
      labelText: "E-mail", focusColor: Colors.indigo, alignLabelWithHint: true);
  final InputDecoration password = const InputDecoration(
      labelText: "Password",
      focusColor: Colors.indigo,
      alignLabelWithHint: true);
}
